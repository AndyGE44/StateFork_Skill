interface:
  display_name: "StateFork"
  short_description: "Build projects in StateFork snapshots"
  default_prompt: "Use $statefork to build an app inside a StateFork sandbox, snapshot milestones, and report the snapshot tree."
